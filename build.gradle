apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
	mavenCentral()
}

sourceSets {
	main {
        java {
            srcDir = ["src/FFAlearner"]
        }
    }
    test {
        java {
            srcDirs = ["src/FFAlearner"]
        }
    }
}

dependencies {
	compile 'org.apache.commons:commons-lang3:3.1'
	testCompile "junit:junit:4.12"
}

